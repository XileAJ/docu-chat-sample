/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    images: {
        unoptimized: true
    }
  } 
}

// next.config.js
module.exports = {
    /* outras configurações */
    assetPrefix: './',
    
  }